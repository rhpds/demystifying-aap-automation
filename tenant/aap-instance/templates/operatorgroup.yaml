---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.tenant.username }}-aap-operatorgroup
  namespace: {{ .Values.tenant.username }}-aap
spec:
  targetNamespaces:
    - {{ .Values.tenant.username }}-aap
