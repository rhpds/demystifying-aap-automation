apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.tenant.username }}-aap-admin-password
  namespace: aap-{{ .Values.tenant.username }}
type: Opaque
stringData:
  password: {{ .Values.tenant.password }}
